def connect_to_server(server_ip, nickname, password):
    print(f"Connettendo al server {server_ip} con il nickname {nickname}...")
    # Codice per la connessione al server (da implementare con una libreria adatta)
    print("Connessione riuscita!")
